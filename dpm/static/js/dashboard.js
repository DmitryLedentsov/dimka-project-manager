(function ($, DPM) {
  'use strict';
  var refreshTimer=null;
  function serviceRow(service){var status=service.status||'unknown',action=status==='running'?'stop':'start',actionLabel=status==='running'?'Stop':'Start',runtime=service.pid?DPM.formatUptime(service.uptime_seconds)+'<small>'+DPM.escape(service.memory_mb)+' MB</small>':'—',error=service.last_error||service.project_error;return '<tr class="service-row" data-search="'+DPM.escape((service.project_name+' '+service.name+' '+service.repository_url).toLowerCase())+'"><td><a class="service-identity" href="'+DPM.basePath+'/services/'+service.id+'"><span class="service-glyph"><i></i><i></i></span><span><strong>'+DPM.escape(service.name)+'</strong><small>'+DPM.escape(service.project_name)+'</small></span></a></td><td><span class="status-pill '+DPM.statusClass(status)+'"><i></i><span>'+DPM.escape(status.toUpperCase())+'</span></span>'+(error?'<span class="row-error" title="'+DPM.escape(error)+'">!</span>':'')+'</td><td><div class="repo-cell"><strong>'+DPM.escape(service.branch)+'</strong><small>'+DPM.escape(service.repository_url)+'</small></div></td><td><code class="commit-chip">'+DPM.shortSha(service.deployed_commit)+'</code></td><td><div class="runtime-cell">'+runtime+'</div></td><td><div class="row-actions"><button class="mini-action js-service-action" data-id="'+service.id+'" data-action="'+action+'">'+actionLabel+'</button><a class="mini-action icon-only" href="'+DPM.basePath+'/services/'+service.id+'">↗</a></div></td></tr>';}
  function issueCard(project){return '<article class="issue-card"><div class="issue-symbol">!</div><div><strong>'+DPM.escape(project.name)+'</strong><span>'+DPM.escape(project.deploy_stage||project.deploy_status)+'</span><p>'+DPM.escape(project.last_error||'Deployment failed')+'</p></div><button class="mini-action js-project-deploy" data-id="'+project.id+'">Retry</button></article>';}
  function render(data){$('#metric-services').text(data.stats.services);$('#metric-running').text(data.stats.running);$('#metric-failed').text(data.stats.failed);$('#metric-projects').text(data.projects.length);var health=data.stats.services?Math.round((data.stats.running/data.stats.services)*100):100;$('#health-percent').text(health+'%');var $body=$('#services-body').empty();data.services.forEach(function(service){$body.append(serviceRow(service));});$('#empty-services').toggleClass('hidden',data.services.length>0);$('.service-table').toggleClass('hidden',data.services.length===0);var deploying=data.projects.filter(function(project){return project.deploying||project.deploy_status==='deploying';});if(deploying.length){var active=deploying[0];$('#active-alert-title').text('Deploying '+active.name);$('#active-alert-copy').text((active.deploy_stage||'working').replaceAll('_',' ')+' · '+DPM.shortSha(active.remote_commit));$('#active-alert').removeClass('hidden');}else{$('#active-alert').addClass('hidden');}var $issues=$('#issues-list').empty();data.issues.forEach(function(project){$issues.append(issueCard(project));});$('#issues-panel').toggleClass('hidden',data.issues.length===0);applyFilter();}
  function loadDashboard(silent){if(!silent)$('#refresh-dashboard').addClass('rotating');DPM.api('GET','/dashboard').done(render).always(function(){$('#refresh-dashboard').removeClass('rotating');});}
  function applyFilter(){var query=String($('#global-filter').val()||'').toLowerCase().trim();$('.service-row').each(function(){$(this).toggle(!query||String($(this).data('search')).indexOf(query)!==-1);});}
  $(document).on('click','.js-service-action',function(event){event.preventDefault();var $button=$(this).prop('disabled',true);DPM.api('POST','/services/'+$button.data('id')+'/'+$button.data('action'),{}).done(function(){DPM.toast('Service command completed','success');loadDashboard(true);}).always(function(){$button.prop('disabled',false);});});
  $(document).on('click','.js-project-deploy',function(){var $button=$(this).prop('disabled',true);DPM.api('POST','/projects/'+$button.data('id')+'/deploy',{}).done(function(){DPM.toast('Deployment queued','success');loadDashboard(true);}).always(function(){$button.prop('disabled',false);});});
  $('#refresh-dashboard').on('click',function(){loadDashboard(false);});$('#global-filter').on('input',applyFilter);$(document).on('dpm:project-added',function(){setTimeout(function(){loadDashboard(true);},350);});loadDashboard(true);refreshTimer=setInterval(function(){loadDashboard(true);},4000);$(window).on('beforeunload',function(){clearInterval(refreshTimer);});
})(window.jQuery, window.DPM);
